<div class="container my-5">
 <div class="row">
    <!-- ジャンルのサイドバーを表示 -->
      <%= render "public/items/genre", genres: @genres %>

    <div class="col-md-8"><!---xs-9 col-xs-offset-1-->
        <h4 style="font-family: HiraMinPro-W6; color: #000000; margin: 20px 0; ">ようこそ、NaganoCakeへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。<br>
        </h4>

        <h3 class="recommended-items mt-5" style="font-family: HiraMinPro-W6; color: #0000000">新着商品</h3>

        <!-- 商品を4つ表示するようにする -->
        <div class="col-xs-offset-3 mt-4"><!---xs-9 col-xs-offset-1-->
            <div class="item--card-content d-flex align-items-start">
                <% @items.last(4).each do |item| %>
                    <div class="col-xs-3 offset-md-3 mx-auto" style="padding-right: 0px; padding-left: 0px;">
                        <%= link_to public_item_path(item.id) ,style: "color: black;", data: {"turbolinks" => false} do %>
                            <%= attachment_image_tag item, :image, :fill, 180, 125, format: 'jpeg',fallback: "no_image.jpg" %><br>
                            <%= item.name %><br>
                            ¥<%= item.price.to_s(:delimited, delimiter: ',') %>
                        <% end %>
                    </div>
                <% end %>
            <div>
        </div>

    </div>
 </div>
     <div class="mt-5">
      <h3><%= link_to "全ての商品を見る>", public_items_path %></h3>
     </div>
</div>